import './story_styles.css'

const preview = {
  parameters: {
    docs: {
      toc: true,
    },
    options: {
      storySort: {
        order: ['Rehowl', 'Hooks', 'Components'],
      },
    },
  },
}

export default preview
